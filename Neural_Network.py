class Neural_Network:
    def __init__(self):
        self.layers=None
        
def evaluate(input,expected):
    signal=input
    for layer in self.layers:
        signal=layer.evaluate(signal) # Evaluate layer by layer
    if(pected not None):
        learnFrom(pected)
        cost=self.cost_function.get_total(expected,signal)
        return Result(signal,cost)
    return Result(signal)